spring:
  application.name: render-be
  cloud:
    config:
      uri: http://localhost:8888
      password: changeMe
      username: user

---
spring:
  config.activate.on-profile: docker
  cloud.config.uri: http://configurator:8888
